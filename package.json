{
  "name": "bogo-buddy",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "mkdir -p dist && cp index-static.html dist/index.html",
    "dev": "npx serve dist"
  },
  "engines": {
    "node": ">=18"
  }
}
